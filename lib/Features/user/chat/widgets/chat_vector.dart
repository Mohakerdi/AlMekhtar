import 'package:flutter/material.dart';
class ChatVector extends StatelessWidget {
  const ChatVector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
