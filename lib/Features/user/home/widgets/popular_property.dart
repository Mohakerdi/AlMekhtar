import 'package:flutter/material.dart';

class PopularProperty extends StatelessWidget {
  const PopularProperty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(

    );
  }
}
